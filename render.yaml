services:
  - type: static
    name: subsaver-frontend
    buildCommand: npm run build
    publishDirectory: dist
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html

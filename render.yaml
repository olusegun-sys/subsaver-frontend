services:
  - type: static
    name: subsaver-frontend
    buildCommand: npm run build
    publishDirectory: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
